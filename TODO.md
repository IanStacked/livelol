# TODO - leaguehelper

Backlog for the notice -> record -> auto-complete pipeline. Line format:
`- [ ] (id) desc <!-- where:file:line found:DATE by:who -->`
`/complete-todos` sweeps this and auto-clears only trivial (T0/T1) items.

## Open

- [ ] (db-migrate-update-cmd) bot.py `!update` still queries Firestore directly (raw stream + doc.reference.update); migrate it through db_service like the cogs now do <!-- where:bot.py:109 found:2026-07-12 by:claude -->
- [ ] (untrack-del-nested-key) untrack_user/untrack_all_users do `del data["server_info.<gid>"]` on a to_dict() result, but Firestore stores that dotted path as a NESTED dict, so the flat key does not exist -> KeyError on multi-guild untrack, swallowed into a generic DatabaseError <!-- where:utils/db_service.py:102 found:2026-07-12 by:claude -->
- [ ] (untrack-usernotfound-wrapped) UserNotFoundError raised inside untrack_user is caught by the broad `except Exception` and re-wrapped as DatabaseError, so "not being tracked" surfaces as a generic DB write failure to the user <!-- where:utils/db_service.py:138 found:2026-07-12 by:claude -->
- [ ] (dodge-update-type) DEFERRED (user 2026-07-12: needs investigation first). New ranked-update type for dodges: after an LP change, check whether the match's gameStartTimestamp/gameEndTimestamp (InfoDto in MatchDto) lines up with the change; won't catch games played at 0 LP, no cheap fix that keeps API calls reasonable. NOTE: lp-streaks now stores last_match_id per user, which is the natural hook for detecting "LP changed but no new game" = dodge <!-- where:cogs/background.py found:2026-07-11 by:user -->
- [ ] (extract-match-info-unbound) extract_match_info binds target_champion/target_kda/win only inside the participant loop; if the target puuid is absent from participants they are never assigned -> UnboundLocalError. Latent (get_recent_match_info always includes the tracked puuid), but a renamed/transferred account edge could trip it <!-- where:utils/helpers.py:60 found:2026-07-12 by:claude -->
- [ ] (namechange-log-both-new) background name-change log reassigns `riot_id = new_riot_id` before the log line, so "Name Change Detected: X -> Y" prints Y -> Y (old name lost). Cosmetic; capture old name before reassigning <!-- where:cogs/background.py:122 found:2026-07-12 by:claude -->
- [ ] (update-cmd-no-streaks) bot.py !update passes no streak to MatchDetailsView (defaults to 0 -> never shows the streak line) and does not persist streak/last_match_id; fold this in when db-migrate-update-cmd routes !update through db_service <!-- where:bot.py:152 found:2026-07-12 by:claude -->

## Done

- [x] (lp-streaks) Consecutive win/loss streaks from the match `win` field, stored as a signed `streak` on the tracked-user doc (+wins / -losses); surfaced in the update embed as "🔥 N-game win streak" / "❄️ N-game loss streak" once |streak| >= 3 (STREAK_DISPLAY_THRESHOLD). `last_match_id` stored so a repeat match (LP change with no new game, e.g. a dodge) doesn't double-count. helpers `next_streak`/`streak_label` unit-tested <!-- where:cogs/background.py found:2026-07-11 by:user done:2026-07-12 branch:feat/lp-streaks -->
- [x] (non-soloduo-matches) DESCOPED (user 2026-07-12: "don't need to track flex"). Bot stays solo/duo only (RANKED_SOLO_5x5 + queue=420) <!-- where:cogs/background.py found:2026-07-11 by:user done:2026-07-12 disposition:descoped -->
- [x] (cog-error-handling) Standardized: commands raise typed LiveLOLError and let on_command_error render; removed catch-log-reraise double-logging in track.py; background_update_task now guards each user so one failure doesn't abort the cycle <!-- where:cogs/ found:2026-07-11 by:user done:2026-07-12 pr:refactor/standardize-cog-errors disposition:refactor-pr -->
- [x] (mgmt-error-return-type) on_command_error annotated `discord.Message | None`; also fixed a BotMissingPermissions fall-through so a handled missing-perms error no longer leaks into the generic handler <!-- where:cogs/management.py:17 found:2026-07-11 by:adversarial-reviewer done:2026-07-12 pr:refactor/standardize-cog-errors disposition:refactor-pr -->
- [x] (db-service-soc) Separation-of-concerns: db_service takes plain ids/dicts, never discord ctx/Guild objects <!-- where:utils/db_service.py found:2026-07-11 by:user done:2026-07-12 pr:refactor/db-service-soc disposition:refactor-pr -->
- [x] (db-migrate-admin) admin.py set_guild_config now goes through db_service (also fixed a latent self.bot.db / missing-await bug that silently no-op'd !updateshere) <!-- where:cogs/admin.py found:2026-07-11 by:user done:2026-07-12 pr:refactor/db-service-soc disposition:refactor-pr -->
- [x] (db-migrate-background) background.py update loop reads via get_all_tracked_users and writes via update_ranked_data; leaderboard.py read migrated too <!-- where:cogs/background.py found:2026-07-11 by:user done:2026-07-12 pr:refactor/db-service-soc disposition:refactor-pr -->
- [x] (command-type-hints) Add type hints ("typing flairs") to all commands <!-- where:cogs/ found:2026-07-11 by:user done:2026-07-11 commit:86d4fbf disposition:auto-digest -->
- [x] (remote-slug) Repoint git remote + PROJECT.yaml repo: to canonical IanStacked/livelol slug; old league-draft-prep-helper only redirects <!-- where:PROJECT.yaml:6 found:2026-07-10 by:daily-brief done:2026-07-10 disposition:auto-silent -->
- [x] (status-stale-heartbeat) Remove stale STATUS.md Next-up #1 (add Heartbeat check to ci.yml); the check already exists in the deploy log-verification <!-- where:.github/workflows/ci.yml:109 found:2026-07-10 by:daily-brief done:2026-07-10 disposition:auto-silent -->
